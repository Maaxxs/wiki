# Linux IDs

- euid: access rights for a process (effective)
- ruid: indicates who initiated a process (real)
- saved user id: a process can switch its euid or ruid to the saved uid during execution
- filesystem used id (fsuid): defines the user accessing the filesystem

